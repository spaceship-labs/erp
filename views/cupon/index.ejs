<script type='text/javascript'>
	window.cupons = <%- JSON.stringify(cupons) %>;
	window.hotels = <%- JSON.stringify(hotels) %>;
	window.user = <%- JSON.stringify(user) %>;
	window.tours = <%- JSON.stringify(tours) %>;
	window.transfers = <%- JSON.stringify(transfers) %>;
</script>
<div ng-controller='cuponCTL'>
    <p>
    	<button class="btn btn-primary pull-right" data-toggle="modal" data-target="#myModal"><i class='fa fa-plus'></i> <%= __('cupon_new')%></button>
    	<button class="btn btn-primary pull-right" data-toggle="modal" data-target="#importmodal"><span class="fa fa-plus"></span> Importar</button>
    </p>
    <br>
    <div table-helper objects='cupons' columns='content.cupons' route='cupon'></div>
	<div class="modal fade" id="myModal">
		<div class="modal-dialog">
			<div class="modal-content">
				<div class="widgetbox personal-information ng-form-helper">
					<%- partial('partial/customform') -%>
				</div>
				<!--<div 
					form-helper 
			        fields='content.cupons' 
			        form-footer='<%= __("cupon_add") %>'
			        form-save = 'createCupon'
			        object = 'cupons'
					objects='{hotels:hotels,tours:tours,transfers:transfers}' 
			        form-title='<%= __("cupon_add") %>'
			       	modal='true'
			       	currency='company.base_currency'
			        >
			    </div>-->
			</div>
		</div>
	</div>
	<div class="modal fade" id="importmodal">
	    <div class="modal-dialog">
	        <div class="modal-content">
	            <div class="modal-header">
	                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
	                <h4 class="modal-title">Importar cupones</h4>
	            </div>
	            <div import-files import-type='cupon' url='/cupon/edit'></div>
	        </div>
	    </div>
	</div>
</div>
